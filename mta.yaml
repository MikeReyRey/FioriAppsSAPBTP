_schema-version: "3.2"
ID: FioriOdata
version: 0.0.1
modules:
- name: FioriOdata-destination-content
  type: com.sap.application.content
  requires:
  - name: FioriOdata-destination-service
    parameters:
      content-target: true
  - name: FioriOdata_html_repo_host
    parameters:
      service-key:
        name: FioriOdata_html_repo_host-key
  - name: uaa_FioriOdata
    parameters:
      service-key:
        name: uaa_FioriOdata-key
  parameters:
    content:
      instance:
        destinations:
        - Name: products_FioriOdata_html_repo_host
          ServiceInstanceName: FioriOdata-html5-app-host-service
          ServiceKeyName: FioriOdata_html_repo_host-key
          sap.cloud.service: products
        - Authentication: OAuth2UserTokenExchange
          Name: products_uaa_FioriOdata
          ServiceInstanceName: FioriOdata-xsuaa-service
          ServiceKeyName: uaa_FioriOdata-key
          sap.cloud.service: products
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: FioriOdata-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: FioriOdata_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nsproducts.zip
      name: nsproducts
      target-path: resources/
- name: nsproducts
  type: html5
  path: products
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: FioriOdata-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: FioriOdata-destination-service
    service-plan: lite
- name: FioriOdata_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: FioriOdata-html5-app-host-service
    service-plan: app-host
- name: uaa_FioriOdata
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: FioriOdata-xsuaa-service
    service-plan: application
parameters:
  deploy_mode: html5-repo
